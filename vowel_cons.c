#include<stdio.h>

int main()

{
 char ch = 'z';
 if (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u') {
 		 printf("%c is a vowel\n", ch);

 }
 else {
 		 printf("%c is a constant\n", ch);
 }
 return 0;


}
